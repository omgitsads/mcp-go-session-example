{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug MCP Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd",
            "args": ["server"],
            "env": {
                "REDIS_ADDR": "localhost:6379",
                "MCP_HOST": "localhost",
                "MCP_PORT": "8080"
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}